# ============================================================================
# CogniWeb Agent - Direct dependencies (compile with pip-compile)
# ============================================================================
# requirements.txt is generated from this file via `make update-deps`
# (pip-compile --generate-hashes requirements.in -o requirements.txt).
# ============================================================================

# Core Dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# LLM API
openai>=1.0.0
httpx>=0.25.0

# Browser Automation
playwright>=1.40.0

# Retry Logic
tenacity>=8.2.0

# ============================================================================
# Post-Installation
# ============================================================================
# After installing, run:
#   playwright install chromium
# ============================================================================
