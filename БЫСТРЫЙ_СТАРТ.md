# 🚀 Быстрый Старт - Исправленная Версия

## ✅ Что исправлено

1. ✅ **OpenAI SDK**: Используется правильный синтаксис `OpenAI()` + `httpx.Client(proxy=...)`
2. ✅ **load_dotenv()**: Переменные окружения загружаются до создания Config
3. ✅ **Прокси VirtualBox**: Правильный адрес `10.0.2.2:7897` для Clash Verge
4. ✅ **Таймауты**: Увеличены до 60 секунд для медленного соединения
5. ✅ **Анти-бот**: Удалён `navigator.webdriver` и другие признаки автоматизации
6. ✅ **Обработка ошибок**: Graceful shutdown и retry логика

## 📦 Установка (5 минут)

### 1. Проверьте Clash Verge на Windows

```powershell
# В PowerShell:
# 1. Убедитесь что Clash Verge запущен
# 2. Проверьте порт (Settings -> General -> Mixed Port = 7897)
# 3. Включите "Allow LAN" в настройках
```

### 2. Установите зависимости в Ubuntu

```bash
# Установите Python пакеты
pip install -r requirements_fixed.txt --break-system-packages

# Установите браузер
playwright install chromium
playwright install-deps  # Может потребовать sudo
```

### 3. Настройте .env файл

```bash
# Создайте .env из шаблона
cp .env.fixed .env

# Откройте и заполните
nano .env

# ОБЯЗАТЕЛЬНО укажите:
OPENAI_API_KEY=ваш_ключ_от_z_ai
PROXY_URL=http://10.0.2.2:7897
```

### 4. Протестируйте настройку

```bash
# Тест 1: Проверка прокси (должен показать ваш IP)
python test_proxy.py

# Тест 2: Проверка API (должен вернуть "Hello, I am working!")
python test_api.py

# Если оба теста прошли - всё готово! ✓
```

### 5. Запустите агента

```bash
python main_fixed.py

# Введите задачу, например:
# "Go to google.com and search for 'Python tutorials'"
```

## 🔧 Если что-то не работает

### Проблема: test_proxy.py не работает

```bash
# Проверьте подключение вручную:
curl --proxy http://10.0.2.2:7897 https://google.com

# Если не работает:
# 1. Проверьте порт в Clash Verge (Settings -> General)
# 2. Убедитесь что "Allow LAN" включен
# 3. Проверьте брандмауэр Windows
```

### Проблема: test_api.py не работает

```bash
# Проверьте API ключ:
cat .env | grep OPENAI_API_KEY

# Если пустой или неправильный:
nano .env  # Исправьте ключ
```

### Проблема: Браузер не запускается

```bash
# Установите системные зависимости:
sudo playwright install-deps
```

## 📝 Пример использования

```bash
$ python main_fixed.py

Enter task description: Go to google.com and search for 'Playwright'
Enter starting URL: https://google.com

# Агент начнёт работу:
STEP 1/15
💭 Thought: I see a search input box with ID 5. I'll type 'Playwright' into it.
🎯 Action: type
✓ Action successful: Typed 'Playwright' into input

STEP 2/15
💭 Thought: Now I see a search button with ID 8. I'll click it.
🎯 Action: click
✓ Action successful: Clicked button: Search

STEP 3/15
💭 Thought: The search results are now visible. Task is complete.
🎯 Action: done

✓✓✓ TASK COMPLETED SUCCESSFULLY! ✓✓✓
```

## 📞 Поддержка

Если проблема не решается, смотрите подробное руководство в файле **ИСПРАВЛЕНИЯ.md**

Там есть:
- Детальное описание всех исправлений
- Диагностика всех возможных ошибок
- Пошаговые инструкции по устранению проблем
- Тестовые скрипты для каждого компонента

Удачи! 🎉
