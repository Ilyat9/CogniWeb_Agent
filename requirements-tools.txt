# ============================================================================
# Optional [tools] extra: heavy/optional capabilities that must stay out of
# the base requirements.txt. Everything here is imported LAZILY - the agent
# runs (with a logged warning + fallback) when none of it is installed.
#   pip install -r requirements-tools.txt
# ============================================================================

# Task 4 (stealth mode): fuller anti-fingerprinting patch set on top of the
# built-in init scripts. Without it: one warning per run, built-in patches
# still apply (see src/infrastructure/browser.py::_stealth_async).
playwright-stealth>=1.0.6

# Task 3 (Crawl4AI approach): higher-quality HTML->Markdown conversion for
# extract_page_content. Without it: the built-in heuristic cleaner is used
# (see src/utils/extract.py). NOTE: crawl4ai is only used as an HTML
# converter here - it never launches its own browser runtime.
crawl4ai>=0.4.0
